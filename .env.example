# Copy this file to `.env` and set real values before running in production
FLASK_SECRET=replace-with-a-random-secret
# Optional: configuration for database URL if moving off SQLite
# DATABASE_URL=sqlite:///app.db
